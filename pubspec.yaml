name: flutter_dio_package
description: "A new Flutter package project."
version: 0.0.1
publish_to: 'none' 

environment:
  sdk: ^3.12.2
  flutter: ">=1.17.0"

dependencies:
  dartz: ^0.10.1
  dio: ^5.10.0
  equatable: ^2.0.8
  flutter:
    sdk: flutter
  
  flutter_core_package:
    git: https://github.com/The-White-Crow/lorawan-flutter-core-package

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:

